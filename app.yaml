runtime: nodejs
vm: true

env_variables:
  # --REQUIRED--
  DATABASE_URI: mongodb://a:a@ds123500.mlab.com:23500/restrunt
  APP_ID: <your-app-id>
  MASTER_KEY: <your-master-key>
  # --OPTIONAL--
  # FILE_KEY: <your-file-key>
  # PARSE_MOUNT: /parse
  # CLOUD_CODE_MAIN:  
